// 점수가 보이는 곳(HR/매니저 화면)마다 반복해서 띄우는 해상도 경고.
//
// 앵커(RUBRIC_FACTUAL/RUBRIC_EMOTIONAL, api/score.js)가 실제로 안정적으로 구분하는
// 건 저점대/중간대/고점대 3단계 수준이지, 0~100 사이 정수 하나하나가 아니다. 그런데
// 화면엔 "62점"처럼 정밀해 보이는 숫자가 그대로 나가서, 근소한 차이(62 vs 65)를
// 실제 우열로 오독하기 쉽다 — "3/6개월 심사 시점에만 조심하면 된다"가 아니라 점수가
// 보이는 모든 곳에 필요한 경고라 별도 컴포넌트로 뺐다. 숫자 자체를 없애지 않은 이유는
// 여러 주에 걸친 평균·추이(성장 궤적) 계산엔 이 해상도가 필요하기 때문 — 개별 노이즈는
// 평균 내면 상쇄되지만, 낱개로 볼 때는 그 상쇄가 안 일어난다는 걸 알려야 한다.
export default function ScoreCaveat() {
  return (
    <div className="locked" style={{ marginBottom: 14 }}>
      ⚠️ 점수는 참고용 추정치입니다. 근소한 차이(예: 62점과 65점)는 실제 역량 차이가
      아니라 AI 채점의 해상도 한계일 수 있습니다 — 개별 숫자보다 여러 주에 걸친
      추이·평균으로 판단해 주세요.
    </div>
  );
}
